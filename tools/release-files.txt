# Files published by tools/publish-release.ps1.
#
# One path or glob per line, relative to the repository root. Blank lines and
# lines starting with # are ignored. Keep this list curated: the build tree also
# contains diagnostic ROMs and stale images that should not reach end users.
#
# Recognised kinds are inferred from the extension:
#   .sof/.rbf -> fpga-sram    .jic/.pof/.rpd -> fpga-flash
#   .64k/.rom -> bios         .ima/.img -> floppy

# These three are what an end user needs, and all three are gitignored, so a
# release is the only way they reach anybody. A name here that does not exist is
# now a hard error rather than a warning -- see the note in publish-release.ps1.
output_files/gertieboard.sof
output_files/gertieboard.jic
tools/gertieboard_bios.64k

# The same bitstream in formats openFPGALoader can read, so the board can be
# programmed from macOS or Linux without Quartus installed at all. .rbf
# configures the FPGA for this session; .rpd is written to the config flash and
# survives a power cycle.
output_files/gertieboard.rbf
output_files/gertieboard.rpd
