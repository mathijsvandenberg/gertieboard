# Project summary

Replace the current summary. Pick one — the field is short, so the first is the one to use.

**Recommended (193 characters):**

> A real NEC V20 in a socket, and every other chip of an IBM PC/XT built in an FPGA.
> Boots MS-DOS 4.01 from a USB disk at 10 MHz, with CGA, EGA and a PS/2 keyboard.
> Built as a gift for my father.

**Shorter (118):**

> A genuine NEC V20 CPU with the entire PC/XT chipset in an FPGA. Boots MS-DOS 4.01
> from a USB disk, at 10 MHz.

**If the title is also editable**, the current one is fine but slightly misleading —
the CPU fitted is a V20, not an 8088. `Gertieboard — a PC/XT with a real CPU and an
FPGA for a chipset` reads better and still carries the keywords.

## Tags

Keep the existing ones (`hardware`, `ongoing project`, `8088`, `DOS`, `P2120`, `RETRO`,
`V20`, `x86`) and add: `fpga`, `vhdl`, `retrocomputing`, `pc-xt`, `bios`, `usb`, `ega`,
`cga`, `de0-nano`, `open hardware`.

`retrocomputing` matters — it is how people browse the contest.

## Links to add (Links section)

| Label | URL |
|---|---|
| Source, documentation and releases | https://github.com/mathijsvandenberg/gertieboard |
| Host loader (serves floppy images over serial) | https://github.com/mathijsvandenberg/gertieboardloader |
| Documentation index | https://github.com/mathijsvandenberg/gertieboard/blob/main/docs/README.md |
| Gotchas — the hard-won lessons | https://github.com/mathijsvandenberg/gertieboard/blob/main/docs/gotchas.md |
